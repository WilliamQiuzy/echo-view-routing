# 组会演示提纲（2026-09-13）

> 面向：导师与项目组成员。目的：展示基线阶梯已在公开数据上跑通，并说明每个数字的含义与边界。

## 1. 已经搭好的东西

- **代码仓库**：GitHub 私有仓库 `WilliamQiuzy/echo-view-routing`，六模块结构（ingest / audit / features / compose / temporal / evaluate），架构文档 `docs/ARCHITECTURE.md`，运行手册 `docs/RUNBOOK.md`。
- **计算资源**：Nebius L40S（与另一个项目共用一张卡，我们的进程通过显存上限隔离），项目目录 `/home/william/echo-view-routing`，数据只在服务器，Mac 只有 6 段示范视频。
- **数据**：EV9V 全量（5,138 段，9 个原始编码）已下载、解压、建清单。原始划分 3,683 / 567 / 888；五类粗粒度任务 3,364 / 521 / 800（排除 PMPALA），与提案一致。
- **标签映射**：`configs/labels.yaml` 冻结为 `family5_v1`，同时保留数据卡与 STFM README 两套互相冲突的英文展开（PMASA / PMVLSA / PMPALA），待临床或作者确认。

## 2. 基线阶梯（提案 §7.1）

| 编号 | 方法 | 状态 |
|---|---|---|
| B-file | 逐文件平均概率，整文件接受/暂缓 | 已跑 |
| B0 | ResNet-18 逐帧分类 + argmax | 已跑（编码器：AdamW 3e-4，早停，最佳为第 1 个 epoch，验证集 cine 级 macro-F1 0.977） |
| B1 | 概率滑动平均 + 滞回 | 已跑 |
| B2 | 粘性 HMM + Viterbi | 已跑 |
| B3 | 左右窗口 JS 散度 + 持续标签变化 | 已跑 |
| B4 | MS-TCN（冻结特征） | 已跑（精简复现，非作者代码） |
| B6 | STFM 官方代码（EV9V 作者） | 已跑：9 类原始任务、视频级，测试集准确率 0.939、macro-F1 0.904（resnet18，早停于第 11 epoch，最佳第 5 epoch） |

**结果表**（run `20260913-035909-baselines-6a612902-s0`，编码器 ckpt_hash `79f4a41af6e3`，测试集 800 个原生 cine、240 条拼接流，阈值在验证集上按 5% 污染率选定后冻结）：

| 方法 | 原生 cine macro-F1 | 逐帧准确率 | 跳变/分钟 | 边界 F1@0.5s | 同类误切（无扰动/有扰动） | 异类漏检（无扰动/有扰动） | 覆盖率@5% | 实际污染率 |
|---|---|---|---|---|---|---|---|---|
| B0 逐帧 argmax | 0.957 | 0.951 | 23.1 | 0.499 | 0.033 / 0.133 | 0.000 / 0.000 | 0.967 | 0.007 |
| B1 平滑+滞回 | 0.949 | 0.968 | 2.4 | 0.918 | 0.000 / 0.017 | 0.017 / 0.017 | 0.999 | 0.014 |
| B2 粘性 HMM | 0.946 | 0.967 | 2.4 | 0.906 | 0.000 / 0.000 | 0.000 / 0.000 | 0.996 | 0.009 |
| B3 JS 散度 | 0.946 | 0.963 | 3.6 | 0.913 | 0.000 / 0.017 | 0.000 / 0.000 | 0.989 | 0.010 |
| B4 MS-TCN（冻结特征） | 0.568 | 0.915 | 0.7 | 0.779 | 0.000 / 0.017 | 0.067 / 0.117 | 0.020 | 0.000 |

B-file（逐文件平均概率，已知文件边界）：测试集 cine 准确率 0.975（多数投票 0.981）；5% 目标下覆盖率 0.998、实际污染率 0.019；1% 目标下覆盖率 0.974、实际污染率 0.011（略超目标，如实报告）。

B4 说明：精简复现的 MS-TCN 在 600 条两段式训练流上训 40 个 epoch（run `20260913-035917-b4_mstcn-56f841e4-s0`）。它最稳定（0.7 跳变/分钟）但 cine 级 macro-F1 只有 0.568：训练流按可用 cine 数量抽样，少数类（A5C、SC4C）样本极少，模型几乎不预测它们；其后验也不校准，5% 策略下只接受 2% 时长。这是**训练数据设计**问题（需要按类平衡的训练流与更多流），不是评分错误；当前不应把它当作"学习式时间模型不如平滑"的证据。

B6 说明：STFM 官方代码在固定 commit `532f60b` 上通过子进程适配器运行（run `20260913-040047-b6_stfm-49f4b5a8-s0`），用其默认的 9 类原始编码任务与视频级评测：测试集准确率 0.939、macro-F1 0.904、macro recall 0.913。与我们的 5 类表**不直接可比**（任务粒度不同、无时间分段指标）；训练参数为 15 epoch 上限（作者默认 100）、batch 32、resnet18，应标注为"适配运行"而非精确复现。它给出的是同一数据上视频级切面识别的参考水平。

完整表与图：`runs/_reports/`（`scripts/report.py` 合并同一编码器下所有阶梯运行）。

**怎么解读这张表（给导师的三句话）**
1. 在整理好的单切面 EV9V 文件上，逐文件分类已接近天花板（97.5%–98%）。时间分段的价值只能来自混合输入，这正是提案第二周 go/no-go 门槛要检验的。
2. 在两段式拼接流上，最简单的平滑（B1）就把边界 F1 从 0.50 提到 0.92、把同类误切压到约 0，且 B1 ≈ B2 ≈ B3。这正是提案预设的"简单方法同样有效"场景；要主张 P0–P2 有贡献，必须在更难的设置（多段流、真实过渡、外部数据）上超过它们。
3. 5% 污染率目标在这个设置下不构成约束（几乎全接受仍只有约 1% 污染）；1% 目标才有区分度（覆盖率降到 0.83–0.84）。下一步应把 1% 作为并列主终点报告。

## 3. 每个指标怎么读

- **native_cine_macroF1 / balAcc**：未修改的测试集 cine，逐 cine 多数投票后的分类质量。这是现有标签能支持的最干净终点。
- **frag_per_min**：单一切面 cine 内的预测跳变次数/分钟。是**稳定性代理指标**，不是真值边界。
- **四格设计**：同类/异类拼接 × 有无 gamma 扰动。`falseSplit_same_*` 是同类拼接处被误切的比例（越低越好）；`missed_diff_*` 是真实类别变化被漏掉的比例（越低越好）。两者要一起看。
- **boundaryF1@0.5s**：语义边界在 ±0.5 s 容差内的一对一匹配 F1。
- **tau@5% / coverage@5% / risk@5%**：阈值只在验证集拼接流上选（目标污染率 5%），然后冻结用于测试集。表中同时给出实际达到的污染率；没达到目标就如实报告。

## 4. 必须说明的边界

1. 拼接流是**特征空间**的拼接（冻结的逐帧编码器下与像素拼接等价），不是真实探头扫查；不能推广到床旁连续录像。
2. 目前是**两段式**拼接（每流 2 段），4–8 段的长流是下一步。
3. 没有患者 ID 映射，不确定性只能按 cine 分组。
4. 编码器在第 1 个 epoch 后即过拟合（验证集分数随后下降），学习率敏感性实验待做。
5. STFM 结果是 9 类视频级分类的适配运行（15 epoch 上限，非作者的 100 epoch），不能当作精确复现或与 5 类表并列比较。

## 5. 想请导师确认的三个问题（同中文摘要）

1. 第一阶段以研究团队的数据整理为目标、接受五类粗粒度范围，是否可行？
2. 能否安排医生核对类别映射（尤其 PSAX 各层面与 PMPALA）？
3. 若两周内时间分段不能证明优于逐文件分类，是否同意调整贡献方向？

## 6. 现场演示命令

```bash
cd ~/Desktop/Harvard/Capstone/echo-view-routing
remote/status.sh              # 服务器与任务状态（可选，需要网络）
.venv/bin/python demo/demo_cli.py   # 离线：表格 + 四格图 + 风险-覆盖率曲线
open runs/<run>/reports/demo_figure.png
open demo/samples/           # 六段示范视频（每个原始编码一段）
```
